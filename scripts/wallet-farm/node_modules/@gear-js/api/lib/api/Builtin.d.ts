import type { GearApi } from '../GearApi';
import type { HexString } from '../types';
export declare class GearBuiltin {
    private _api;
    constructor(_api: GearApi);
    queryId(builtinId: number | bigint): Promise<HexString>;
}
