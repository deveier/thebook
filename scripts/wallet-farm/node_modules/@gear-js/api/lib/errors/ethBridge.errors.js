class AuthoritySetHashError extends Error {
    name = 'AuthoritySetHashError';
    message = 'Unable to get authority set hash';
    constructor(message) {
        super();
        this.message = `${this.message}. ${message}`;
    }
}
class ClearTimerError extends Error {
    name = 'ClearTimerError';
    message = "Can't clear timer";
    constructor(message) {
        super();
        this.message = `${this.message}. ${message}`;
    }
}
class GetQueueMerkleRootError extends Error {
    name = 'GetQueueMerkleRootError';
    message = 'Unable to get queue merkle root';
    constructor(message) {
        super();
        this.message = `${this.message}. ${message}`;
    }
}

export { AuthoritySetHashError, ClearTimerError, GetQueueMerkleRootError };
